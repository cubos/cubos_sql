[package]
name = "cubos_sql_core"
version = "0.1.0"
edition = "2021"
description = "Shared types between cubos_sql runtime and proc macro"

[dependencies]
toml = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
