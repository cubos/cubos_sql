[package]
name = "cubos_sql_core"
version = "0.1.0"
edition = "2024"
description = "Shared types between cubos_sql runtime and proc macro"

[dependencies]
toml = "1"
serde = { version = "1", features = ["derive"] }
thiserror = "2"
sha2 = "0.11"

[dev-dependencies]
serde_json = "1"
tempfile = "3"
