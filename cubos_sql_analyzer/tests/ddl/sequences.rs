//! CREATE / ALTER / DROP SEQUENCE (standalone, not via SERIAL) —
//! **coverage gap**.
//!
//! TODO: CREATE SEQUENCE with START/INCREMENT/MIN/MAX/CACHE, ALTER
//! SEQUENCE, owned-by relation, sequence function return types
//! (`nextval` / `currval` / `lastval` all return int8).
