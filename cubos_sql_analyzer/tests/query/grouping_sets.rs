//! GROUPING SETS / CUBE / ROLLUP — **coverage gap**.
//!
//! TODO: GROUPING SETS explicit, CUBE, ROLLUP, the GROUPING() function,
//! nullability of columns absent from a grouping set (they become NULL),
//! interaction with HAVING, combination with ordinary GROUP BY.
