//! Array operations beyond array-as-column-type — **coverage gap**.
//!
//! TODO: `ANY (arr)` / `ALL (arr)` / `= ANY` in WHERE, `unnest`, array
//! slicing `arr[1:3]`, array concat `||`, `array_agg(expr)` as the main
//! projection, array constructor `ARRAY[...]`, multidimensional arrays,
//! `array_position`/`array_length`/`cardinality` return types.
