//! JSON / JSONB operators beyond `->>` and `?` — **coverage gap**.
//!
//! TODO: containment `@>` / `<@`, key-exists variants `?|` / `?&`,
//! jsonpath operators `@?` / `@@`, `jsonb_path_query` / `jsonb_path_exists`,
//! `#>` / `#>>` path operators, `jsonb_set`/`jsonb_insert` return types,
//! json constructors `json_build_object` / `jsonb_build_array`.
