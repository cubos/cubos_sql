//! CREATE / DROP TRIGGER — **coverage gap**.
//!
//! TODO: CREATE TRIGGER (BEFORE/AFTER/INSTEAD OF, ROW/STATEMENT),
//! trigger functions with RETURN NEW/OLD, constraint triggers, dropping
//! the function a trigger depends on.
